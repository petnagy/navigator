<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.petnagy.navigatordemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".ProjectApplication"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".modules.onboarding.OnboardingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".modules.login.LoginActivity" />

        <activity android:name=".modules.signup.SignUpActivity" />

        <activity android:name=".modules.dashboard.DashboardActivity" />

        <activity android:name=".modules.dashboarddata.DashboardDataActivity" />

        <activity android:name=".modules.userdata.UserDataActivity" />

        <activity android:name=".modules.listdetail.ListDetailActivity" />
    </application>

</manifest>